@* @model CarRental.Models.Car

@{
    ViewData["Title"] = "Добавление автомобиля";
}

@{
    var currentYear = DateTime.Now.Year;  // Сейчас 2026
}

<style>
    :root {
        --text-light: #2c3e50;
        --text-dark: #f8f9fa;
        --text-muted-light: #6c757d;
        --text-muted-dark: #adb5bd;
        --primary-blue: #3498db;
        --primary-dark-blue: #2980b9;
    }

    body {
        background-image: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), 
                          url('https://images.pexels.com/photos/112460/pexels-photo-112460.jpeg?auto=compress&cs=tinysrgb&w=1920');
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        min-height: 100vh;
        margin: 0;
        padding: 0;
    }

    body.dark-mode {
        background-image: linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.8)), 
                          url('https://images.pexels.com/photos/112460/pexels-photo-112460.jpeg?auto=compress&cs=tinysrgb&w=1920');
    }

    /* Шапка */
    .navbar-custom {
        background: linear-gradient(135deg, rgba(44, 62, 80, 0.95), rgba(52, 152, 219, 0.95));
        backdrop-filter: blur(10px);
        padding: 1rem 0;
        margin-bottom: 2rem;
    }

    body.dark-mode .navbar-custom {
        background: linear-gradient(135deg, rgba(30, 43, 58, 0.95), rgba(44, 62, 80, 0.95));
    }

    /* Заголовок страницы */
    .page-header {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 1.5rem 2rem;
        margin-bottom: 2rem;
        box-shadow: 0 15px 35px rgba(0,0,0,0.3);
    }

    body.dark-mode .page-header {
        background: rgba(45, 45, 45, 0.95);
    }

    .page-header h1 {
        color: #2c3e50;
        font-weight: 700;
        margin: 0;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    body.dark-mode .page-header h1 {
        color: white;
    }

    .page-header h1 i {
        color: var(--primary-blue);
    }

    /* Карточка формы */
    .form-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 0 15px 35px rgba(0,0,0,0.3);
    }

    body.dark-mode .form-card {
        background: rgba(45, 45, 45, 0.95);
    }

    .form-header {
        background: linear-gradient(135deg, var(--primary-blue), var(--primary-dark-blue));
        padding: 1.5rem 2rem;
    }

    .form-header h4 {
        color: white;
        margin: 0;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .form-body {
        padding: 2rem;
    }

    /* Поля формы */
    .form-label {
        font-weight: 600;
        color: #2c3e50;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .form-label i {
        color: var(--primary-blue);
    }

    body.dark-mode .form-label {
        color: #f8f9fa;
    }

    .form-control {
        background: rgba(255, 255, 255, 0.9);
        border: 2px solid rgba(52, 152, 219, 0.2);
        border-radius: 12px;
        padding: 0.75rem 1rem;
        transition: all 0.3s ease;
        color: #2c3e50;
        width: 100%;
    }

    /* Стили для placeholder в светлой теме */
    .form-control::placeholder {
        color: #a0aec0;
        opacity: 1;
    }

    body.dark-mode .form-control {
        background: rgba(50, 50, 50, 0.9);
        border-color: rgba(255, 255, 255, 0.15);
        color: white;
    }

    /* Стили для placeholder в темной теме */
    body.dark-mode .form-control::placeholder {
        color: #a0aec0 !important;
        opacity: 0.8 !important;
    }

    /* Для Chrome, Safari, Edge */
    body.dark-mode .form-control::-webkit-input-placeholder {
        color: #a0aec0 !important;
        opacity: 0.8 !important;
    }

    /* Для Firefox */
    body.dark-mode .form-control::-moz-placeholder {
        color: #a0aec0 !important;
        opacity: 0.8 !important;
    }

    /* Для Internet Explorer */
    body.dark-mode .form-control:-ms-input-placeholder {
        color: #a0aec0 !important;
        opacity: 0.8 !important;
    }

    .form-control:focus {
        border-color: var(--primary-blue);
        box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.25);
        background: white;
    }

    body.dark-mode .form-control:focus {
        background: rgba(70, 70, 70, 0.9);
        border-color: var(--primary-blue);
    }

    /* Select */
    select.form-control {
        cursor: pointer;
    }

    /* Input group (для поля с BYN/день) */
    .input-group-text {
        background: linear-gradient(135deg, var(--primary-blue), var(--primary-dark-blue));
        color: white;
        border: none;
        border-radius: 0 12px 12px 0;
        font-weight: 600;
    }

    body.dark-mode .input-group-text {
        background: linear-gradient(135deg, #2c3e50, #34495e);
    }

    /* Чекбокс */
    .form-check {
        padding-left: 1.5rem;
        margin-top: 0.5rem;
    }

    .form-check-input {
        width: 1.2rem;
        height: 1.2rem;
        border-radius: 4px;
        border: 2px solid rgba(52, 152, 219, 0.3);
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .form-check-input:checked {
        background-color: var(--primary-blue);
        border-color: var(--primary-blue);
    }

    body.dark-mode .form-check-input {
        background-color: rgba(60, 60, 60, 0.9);
        border-color: rgba(255, 255, 255, 0.2);
    }

    body.dark-mode .form-check-input:checked {
        background-color: var(--primary-blue);
        border-color: var(--primary-blue);
    }

    .form-check-label {
        color: #2c3e50;
        font-weight: 500;
        margin-left: 0.5rem;
        cursor: pointer;
    }

    body.dark-mode .form-check-label {
        color: #f8f9fa;
    }

    /* Кнопки */
    .btn {
        padding: 0.75rem 2rem;
        border-radius: 50px;
        font-weight: 600;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        border: none;
    }

    .btn-primary {
        background: linear-gradient(135deg, var(--primary-blue), var(--primary-dark-blue));
        color: white;
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(52, 152, 219, 0.4);
        color: white;
    }

    .btn-secondary {
        background: linear-gradient(135deg, #95a5a6, #7f8c8d);
        color: white;
    }

    .btn-secondary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(149, 165, 166, 0.4);
        color: white;
    }

    /* Ошибки валидации */
    .text-danger {
        color: #e74c3c;
        font-size: 0.85rem;
        margin-top: 0.25rem;
        display: block;
    }

    .validation-summary-errors {
        background: rgba(231, 76, 60, 0.1);
        border-left: 4px solid #e74c3c;
        padding: 1rem;
        border-radius: 8px;
        margin-bottom: 1.5rem;
        color: #2c3e50;
    }

    body.dark-mode .validation-summary-errors {
        background: rgba(231, 76, 60, 0.2);
        color: #f8f9fa;
    }

    /* Подсказка */
    .form-text {
        color: #6c757d;
        font-size: 0.8rem;
        margin-top: 0.25rem;
    }

    body.dark-mode .form-text {
        color: #adb5bd;
    }

    @* /* Адаптивность */
    @media (max-width: 768px) {
        .page-header {
            padding: 1rem;
            text-align: center;
        }
        
        .form-body {
            padding: 1rem;
        }
        
        .btn {
            width: 100%;
            justify-content: center;
        }
        
        .d-flex {
            flex-direction: column;
            gap: 0.5rem;
        }
        
        .row > div {
            margin-bottom: 1rem;
        }
    } *@
@* </style>

<!-- Основной контент -->
<div class="container">
    <!-- Заголовок страницы -->
    <div class="page-header">
        <h1>
            <i class="bi bi-car-front"></i> @ViewData["Title"]
        </h1>
    </div>

    <!-- Форма -->
    <div class="form-card">
        <div class="form-header">
            <h4>
                <i class="bi bi-plus-circle"></i> Заполните информацию об автомобиле
            </h4>
        </div>
        <div class="form-body">
            <form asp-action="Create" method="post">
                <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>

                <div class="row mb-4">
                    <div class="col-md-6">
                        <label asp-for="Brand" class="form-label">
                            <i class="bi bi-tag"></i> Марка
                        </label>
                        <input asp-for="Brand" class="form-control" placeholder="Например: Toyota, BMW, Audi" />
                        <span asp-validation-for="Brand" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="Model" class="form-label">
                            <i class="bi bi-tags"></i> Модель
                        </label>
                        <input asp-for="Model" class="form-control" placeholder="Например: Camry, X5, A6" />
                        <span asp-validation-for="Model" class="text-danger"></span>
                    </div>
                </div>

                <div class="row mb-4">
                    <div class="col-md-6">
                        <label asp-for="Year" class="form-label">
                            <i class="bi bi-calendar"></i> Год выпуска
                        </label>
                        <input asp-for="Year" class="form-control"
                               type="number"
                               inputmode="numeric"
                               min="1885"
                               max="@currentYear"
                               value="@currentYear"
                               placeholder="@currentYear" />
                        <span asp-validation-for="Year" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="DailyPrice" class="form-label">
                            <i class="bi bi-currency-dollar"></i> Цена за день (BYN)
                        </label>
                        <div class="input-group">
                            <input asp-for="DailyPrice" class="form-control decimal-input" placeholder="150.00"/>
                            <span class="input-group-text">BYN/день</span>
                        </div>
                        <span asp-validation-for="DailyPrice" class="text-danger"></span>
                        <small class="form-text">
                            <i class="bi bi-info-circle"></i> Введите цену в формате 150.00
                        </small>
                    </div>
                </div>

                <div class="row mb-4">
                    <div class="col-md-6">
                        <div class="form-check">
                            <input asp-for="IsAvailable" class="form-check-input" />
                            <label asp-for="IsAvailable" class="form-check-label">
                                <i class="bi bi-check-circle"></i> Автомобиль доступен для аренды
                            </label>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="CategoryId" class="form-label">
                            <i class="bi bi-tags"></i> Категория
                        </label>
                        <select asp-for="CategoryId" class="form-control" asp-items="ViewBag.Categories">
                            <option value="">-- Выберите категорию --</option>
                        </select>
                        <span asp-validation-for="CategoryId" class="text-danger"></span>
                    </div>
                </div>

                <div class="d-flex justify-content-between align-items-center">
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Назад
                    </a>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save"></i> Сохранить
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
} *@ 






















@model CarRental.Models.Car

@{
    ViewData["Title"] = "Добавление автомобиля";
    var currentYear = DateTime.Now.Year;
    var features = ViewBag.Features as List<CarRental.Models.Feature> ?? new List<CarRental.Models.Feature>();
}

<style>
    :root {
        --text-light: #2c3e50;
        --text-dark: #f8f9fa;
        --text-muted-light: #6c757d;
        --text-muted-dark: #adb5bd;
        --primary-blue: #3498db;
        --primary-dark-blue: #2980b9;
    }

    body {
        background-image: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), 
                          url('https://images.pexels.com/photos/112460/pexels-photo-112460.jpeg?auto=compress&cs=tinysrgb&w=1920');
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        min-height: 100vh;
        margin: 0;
        padding: 0;
    }

    body.dark-mode {
        background-image: linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.8)), 
                          url('https://images.pexels.com/photos/112460/pexels-photo-112460.jpeg?auto=compress&cs=tinysrgb&w=1920');
    }

    /* Шапка */
    .navbar-custom {
        background: linear-gradient(135deg, rgba(44, 62, 80, 0.95), rgba(52, 152, 219, 0.95));
        backdrop-filter: blur(10px);
        padding: 1rem 0;
        margin-bottom: 2rem;
    }

    body.dark-mode .navbar-custom {
        background: linear-gradient(135deg, rgba(30, 43, 58, 0.95), rgba(44, 62, 80, 0.95));
    }

    /* Заголовок страницы */
    .page-header {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 1.5rem 2rem;
        margin-bottom: 2rem;
        box-shadow: 0 15px 35px rgba(0,0,0,0.3);
    }

    body.dark-mode .page-header {
        background: rgba(45, 45, 45, 0.95);
    }

    .page-header h1 {
        color: #2c3e50;
        font-weight: 700;
        margin: 0;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    body.dark-mode .page-header h1 {
        color: white;
    }

    .page-header h1 i {
        color: var(--primary-blue);
    }

    /* Карточка формы */
    .form-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 0 15px 35px rgba(0,0,0,0.3);
    }

    body.dark-mode .form-card {
        background: rgba(45, 45, 45, 0.95);
    }

    .form-header {
        background: linear-gradient(135deg, var(--primary-blue), var(--primary-dark-blue));
        padding: 1.5rem 2rem;
    }

    .form-header h4 {
        color: white;
        margin: 0;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .form-body {
        padding: 2rem;
    }

    /* Поля формы */
    .form-label {
        font-weight: 600;
        color: #2c3e50;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .form-label i {
        color: var(--primary-blue);
    }

    body.dark-mode .form-label {
        color: #f8f9fa;
    }

    .form-control, select.form-control {
        background: rgba(255, 255, 255, 0.9);
        border: 2px solid rgba(52, 152, 219, 0.2);
        border-radius: 12px;
        padding: 0.75rem 1rem;
        transition: all 0.3s ease;
        color: #2c3e50;
        width: 100%;
    }

    .form-control::placeholder {
        color: #a0aec0;
        opacity: 1;
    }

    body.dark-mode .form-control, body.dark-mode select.form-control {
        background: rgba(50, 50, 50, 0.9);
        border-color: rgba(255, 255, 255, 0.15);
        color: white;
    }

    body.dark-mode .form-control::placeholder {
        color: #a0aec0 !important;
        opacity: 0.8 !important;
    }

    .form-control:focus, select.form-control:focus {
        border-color: var(--primary-blue);
        box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.25);
        background: white;
    }

    body.dark-mode .form-control:focus, body.dark-mode select.form-control:focus {
        background: rgba(70, 70, 70, 0.9);
        border-color: var(--primary-blue);
    }

    select.form-control {
        cursor: pointer;
    }

    /* Секция характеристик */
    .features-section {
        margin-top: 2rem;
        margin-bottom: 2rem;
        padding-top: 1.5rem;
        border-top: 2px solid rgba(52, 152, 219, 0.2);
    }

    .features-title {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--primary-blue);
        margin-bottom: 1.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 1rem;
    }

    /* Input group */
    .input-group-text {
        background: linear-gradient(135deg, var(--primary-blue), var(--primary-dark-blue));
        color: white;
        border: none;
        border-radius: 0 12px 12px 0;
        font-weight: 600;
    }

    body.dark-mode .input-group-text {
        background: linear-gradient(135deg, #2c3e50, #34495e);
    }

    /* Чекбокс */
    .form-check {
        padding-left: 1.5rem;
        margin-top: 0.5rem;
    }

    .form-check-input {
        width: 1.2rem;
        height: 1.2rem;
        border-radius: 4px;
        border: 2px solid rgba(52, 152, 219, 0.3);
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .form-check-input:checked {
        background-color: var(--primary-blue);
        border-color: var(--primary-blue);
    }

    body.dark-mode .form-check-input {
        background-color: rgba(60, 60, 60, 0.9);
        border-color: rgba(255, 255, 255, 0.2);
    }

    body.dark-mode .form-check-input:checked {
        background-color: var(--primary-blue);
        border-color: var(--primary-blue);
    }

    .form-check-label {
        color: #2c3e50;
        font-weight: 500;
        margin-left: 0.5rem;
        cursor: pointer;
    }

    body.dark-mode .form-check-label {
        color: #f8f9fa;
    }

    /* Кнопки */
    .btn {
        padding: 0.75rem 2rem;
        border-radius: 50px;
        font-weight: 600;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        border: none;
    }

    .btn-primary {
        background: linear-gradient(135deg, var(--primary-blue), var(--primary-dark-blue));
        color: white;
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(52, 152, 219, 0.4);
        color: white;
    }

    .btn-secondary {
        background: linear-gradient(135deg, #95a5a6, #7f8c8d);
        color: white;
    }

    .btn-secondary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(149, 165, 166, 0.4);
        color: white;
    }

    /* Ошибки валидации */
    .text-danger {
        color: #e74c3c;
        font-size: 0.85rem;
        margin-top: 0.25rem;
        display: block;
    }

    .validation-summary-errors {
        background: rgba(231, 76, 60, 0.1);
        border-left: 4px solid #e74c3c;
        padding: 1rem;
        border-radius: 8px;
        margin-bottom: 1.5rem;
        color: #2c3e50;
    }

    body.dark-mode .validation-summary-errors {
        background: rgba(231, 76, 60, 0.2);
        color: #f8f9fa;
    }

    /* Подсказка */
    .form-text {
        color: #6c757d;
        font-size: 0.8rem;
        margin-top: 0.25rem;
    }

    body.dark-mode .form-text {
        color: #adb5bd;
    }

    /* Адаптивность */
    @* @media (max-width: 768px) {
        .page-header {
            padding: 1rem;
            text-align: center;
        }
        
        .form-body {
            padding: 1rem;
        }
        
        .btn {
            width: 100%;
            justify-content: center;
        }
        
        .d-flex {
            flex-direction: column;
            gap: 0.5rem;
        }
        
        .row > div {
            margin-bottom: 1rem;
        }
        
        .features-grid {
            grid-template-columns: 1fr;
        }
    } *@
</style>

<!-- Основной контент -->
<div class="container">
    <!-- Заголовок страницы -->
    <div class="page-header">
        <h1>
            <i class="bi bi-car-front"></i> @ViewData["Title"]
        </h1>
    </div>

    <!-- Форма -->
    <div class="form-card">
        <div class="form-header">
            <h4>
                <i class="bi bi-plus-circle"></i> Заполните информацию об автомобиле
            </h4>
        </div>
        <div class="form-body">
            <form asp-action="Create" method="post">
                <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>

                <div class="row mb-4">
                    <div class="col-md-6">
                        <label asp-for="Brand" class="form-label">
                            <i class="bi bi-tag"></i> Марка
                        </label>
                        <input asp-for="Brand" class="form-control" placeholder="Например: Toyota, BMW, Audi" />
                        <span asp-validation-for="Brand" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="Model" class="form-label">
                            <i class="bi bi-tags"></i> Модель
                        </label>
                        <input asp-for="Model" class="form-control" placeholder="Например: Camry, X5, A6" />
                        <span asp-validation-for="Model" class="text-danger"></span>
                    </div>
                </div>

                <div class="row mb-4">
                    <div class="col-md-6">
                        <label asp-for="Year" class="form-label">
                            <i class="bi bi-calendar"></i> Год выпуска
                        </label>
                        <input asp-for="Year" class="form-control"
                               type="number"
                               inputmode="numeric"
                               min="1885"
                               max="@currentYear"
                               value="@currentYear"
                               placeholder="@currentYear" />
                        <span asp-validation-for="Year" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="DailyPrice" class="form-label">
                            <i class="bi bi-currency-dollar"></i> Цена за день (BYN)
                        </label>
                        <div class="input-group">
                            <input asp-for="DailyPrice" class="form-control decimal-input" placeholder="150.00"/>
                            <span class="input-group-text">BYN/день</span>
                        </div>
                        <span asp-validation-for="DailyPrice" class="text-danger"></span>
                        <small class="form-text">
                            <i class="bi bi-info-circle"></i> Введите цену в формате 150.00
                        </small>
                    </div>
                </div>

                <div class="row mb-4">
                    <div class="col-md-6">
                        <div class="form-check">
                            <input asp-for="IsAvailable" class="form-check-input" />
                            <label asp-for="IsAvailable" class="form-check-label">
                                <i class="bi bi-check-circle"></i> Автомобиль доступен для аренды
                            </label>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="CategoryId" class="form-label">
                            <i class="bi bi-tags"></i> Категория
                        </label>
                        <select asp-for="CategoryId" class="form-control" asp-items="ViewBag.Categories">
                            <option value="">-- Выберите категорию --</option>
                        </select>
                        <span asp-validation-for="CategoryId" class="text-danger"></span>
                    </div>
                </div>

                <!-- Секция характеристик -->
                @if (features.Any())
                {
                    <div class="features-section">
                        <div class="features-title">
                            <i class="bi bi-gear"></i>
                            Характеристики автомобиля
                        </div>
                        <div class="features-grid">
                            @foreach (var feature in features)
                            {
                                <div class="form-group mb-3">
                                    <label class="form-label">
                                        <i class="bi bi-list-check"></i> @feature.Name
                                    </label>
                                    <select name="feature_@feature.Id" class="form-control">
                                        <option value="">-- Выберите @feature.Name.ToLower() --</option>
                                        @if (ViewBag.FeatureValues != null && ViewBag.FeatureValues.ContainsKey(feature.Id))
                                        {
                                            var values = ViewBag.FeatureValues[feature.Id] as List<CarRental.Models.FeatureValue>;
                                            if (values != null)
                                            {
                                                foreach (var value in values)
                                                {
                                                    <option value="@value.Id">@value.Value</option>
                                                }
                                            }
                                        }
                                    </select>
                                </div>
                            }
                        </div>
                    </div>
                }

                <div class="d-flex justify-content-between align-items-center">
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Назад
                    </a>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save"></i> Сохранить
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}