@{
    ViewData["Title"] = "Пароль изменен";
    Layout = "~/Views/Shared/_EmptyLayout.cshtml";
}

<div class="container" style="max-width: 500px;">
    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-body p-5 text-center">
            <div class="mb-4">
                <i class="bi bi-check-circle" style="font-size: 4rem; color: #27ae60;"></i>
            </div>
            <h3>Пароль успешно изменен!</h3>
            <p class="text-muted">@ViewBag.Message</p>
            
            <a href="/Account/AdminLogin" class="btn btn-primary mt-3">
                <i class="bi bi-box-arrow-in-right"></i> Войти в панель управления
            </a>
        </div>
    </div>
</div>

<style>
    .card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
    }
    
    body.dark-mode .card {
        background: rgba(45, 45, 45, 0.95);
    }
    
    .btn-primary {
        background: linear-gradient(135deg, #e74c3c, #c0392b);
        border: none;
        font-weight: 600;
    }
</style>